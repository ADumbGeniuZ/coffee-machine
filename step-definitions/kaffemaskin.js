// Import our program (in this case an instance of a coffee machine so that we can run unit tests methods)
let CoffeeMachine = require('../index.js'); // man skriver ".." för att hoppa upp ett steg från mappen man är i
let myMachine = new CoffeeMachine();


// Export the step-definition(tests) so that Cucumber can read/use them
module.exports = function () {
   

    

  


   

    

    

    
}